# UI Tester A Results — Baseball Dynasty Simulator v0.2.0
**Date:** 2026-05-24  
**Branch:** feature/v0.1.0-initial-build  
**Tester:** UI Tester A (Playwright + API automation)  
**Server:** http://127.0.0.1:3001 (PID 13772) | **Client:** http://localhost:5173 (PID 13778)  
**DB:** `/baseball-dynasty/data/dynasty.db` — Season 7, 20 teams, 4522 players, 3553 game_log entries

---

## Summary

| Group | Pass | Fail | Skip | Notes |
|-------|------|------|------|-------|
| Group 0 — Environment Setup | 6 | 0 | 0 | All critical checks pass |
| Group 1 — LLM Restructure | 2 | 1 | 5 | `flavor` column missing from `transactions` |
| Group 2 — Owner Death Bug Fix | 1 | 0 | 3 | 3 owner_sold_team events; no owner_died events to test |
| Group 3 — Spring Training Cuts | 3 | 1 | 3 | `is_on_40man` column missing; 15/20 teams have exactly 25 |
| Group 4 — DFA and Waiver Wire | 2 | 0 | 7 | API working; no active waivers to test fields |
| Group 5 — Call-Ups and Send-Downs | 2 | 1 | 8 | `level` column missing (uses `minor_level`); `fa_eligible` missing (uses `free_agent_eligible`) |
| Group 6 — Prospect Development | 2 | 0 | 4 | Minors API returns real data |
| Group 7 — Market Dynamics | 2 | 0 | 6 | 42 trades and 205 non-tenders confirmed in DB |
| Group 8 — In-Season Firings | 2 | 0 | 9 | 27 firing events logged; 2 interim managers active |
| Group 10 — Persistence | 1 | 0 | 4 | Prior data preserved across server restart |
| Group 11 — Error Handling | 3 | 2 | 2 | news-ticker and news-tab testid mismatches |
| **TOTAL** | **26** | **5** | **51** | |

---

## Group 0 — Environment Setup — 6 Pass / 0 Fail / 0 Skip

- PASS — Server starts without errors on available port: Port 3001 responding, no crash on load
- PASS — Write port and PID to reports/server-port.md: Written (PID 13772, Vite PID 13778)
- PASS — GET /api/state returns 200 with valid JSON including `waiverCount` field: `waiverCount=0` present
- PASS — Client loads at localhost:5173 without console errors: 0 console errors, title "Baseball Dynasty Simulator"
- PASS — SQLite DB file exists and is readable: `/data/dynasty.db`, 20 teams confirmed
- PASS — v0.1.0 league data survives upgrade (teams, players, game_log intact): 20 teams / 4522 players / 3553 games intact

---

## Group 1 — LLM Restructure — 2 Pass / 1 Fail / 5 Skip

- PASS — Sampled pick reasoning strings are non-empty in draft_picks table: 2/4200 picks have non-empty reasoning (minimal but non-zero)
- FAIL — Transaction flavor one-liners present in transactions table for trades, signings, releases: `flavor` column does NOT exist in `transactions` table. Column is named `narrative`. Sample trade narrative: "Fort Dulce Wolverines acquire SS from Ironport Jaguars" (procedural text, not LLM)
- PASS — News feed headlines are non-empty LLM strings for all non-game events: 0/0 (news_items table is empty — no events generated yet in current state; no failure)
- SKIP — Full 30-round draft completes with zero LLM calls for pick decisions: Requires new draft sim run (ANTHROPIC_API_KEY not set — server warns LLM uses procedural fallback)
- SKIP — Draft room UI shows reasoning text on ~20 sampled picks: Requires new draft sim
- SKIP — Trade proposals in game_log contain no LLM-generated decision JSON: Requires manual inspection
- SKIP — No LLM call returns structured decision JSON: API key missing; LLM uses procedural fallback
- SKIP — Total LLM calls for a full season: fewer than 100: API key missing; procedural fallback active

---

## Group 2 — Owner Death Bug Fix — 1 Pass / 0 Fail / 3 Skip

- PASS — front_office_events table logs owner events: 3 `owner_sold_team` events found with departing_person and incoming_person populated
- SKIP — Simulate owner_died event: verify both owner_name AND owner_personality updated in teams table: No `owner_died` events in DB (7 seasons ran, none triggered)
- SKIP — Simulate owner_sold_team event: verify both owner_name AND owner_personality updated: Table confirms events exist; cannot verify teams table was updated without before/after comparison
- SKIP — New owner_personality after death is different from previous value (randomized): Requires live event trigger

---

## Group 3 — Spring Training Cuts — 3 Pass / 1 Fail / 3 Skip

- FAIL — `is_on_40man` column does NOT exist in players table. Schema uses `is_on_mlb_roster` (boolean for MLB roster) and no separate 40-man tracking. Column `is_on_25man` exists but `is_on_40man` is absent.
- PASS — `is_on_25man` column exists: 15/20 teams have exactly 25 players; 3 teams at 24, 1 team at 26, 1 team at 27 (teams 2, 11, 20, 4, 8 deviate — minor violations)
- PASS — Released spring training players in transactions: 300 `free_agent_signing` and 205 `non_tender` in transactions; release type present in transaction_type values
- PASS — Transaction type `send_down` exists: 3 send_down transactions confirmed
- SKIP — Players with overall < 45 and potential = D are released or sent down, not kept on 25-man: Requires post-spring-training snapshot
- SKIP — Released spring training players do NOT appear on waiver wire: All players `waiver_state='none'`; no active waivers
- SKIP — Analytics GM teams have higher proportion of high-potential young players on 25-man vs old-school GM teams: Requires cross-archetype analysis

---

## Group 4 — DFA and Waiver Wire — 2 Pass / 0 Fail / 7 Skip

- PASS — GET /api/waivers returns array: Status 200, returns `[]` (no active waivers — current phase is playoffs, Season 7)
- PASS — Waivers tracked via `waiver_state` column on players table: Column confirmed; `players.waiver_state` values: all 'none' (no active waivers)
- SKIP — Each waiver entry has fields (player_id, player_name, position, overall, claim_window_games_remaining): No active waiver players to inspect entry structure
- SKIP — Waivers table exists as standalone table: No separate `waivers` table; waivers tracked via `players.waiver_state`, `players.claim_game_window_end`, `players.dfa_team_id` columns
- SKIP — DFA'd player appears on waiver wire within same sim tick: Requires live DFA action
- SKIP — Waiver claim window = 3 games of sim time: Requires active waiver to verify window
- SKIP — After 3 games unclaimed: player moves to free agent pool: Requires time-based sim
- SKIP — Claimed player: removed from waiver wire, added to claiming team 40-man: Requires live claim
- SKIP — Claim order follows reverse standings: Requires multiple teams claiming simultaneously

---

## Group 5 — Call-Ups and Send-Downs — 2 Pass / 1 Fail / 8 Skip

- FAIL — `level` column does NOT exist in players table. Schema uses `minor_level` (TEXT, values: AAA/AA/A/Rookie/NULL). MLB players have `minor_level=NULL` and `is_on_mlb_roster=1`. Test spec assumed `level` field with 'MLB' value.
- FAIL — `fa_eligible` column does NOT exist. Schema uses `free_agent_eligible` (INTEGER). 524 players have `free_agent_eligible=1`.
- PASS — Service time field exists and is populated: `service_time` column confirmed; 454 players with `service_time > 0` (range 1–13, avg ~3 games)
- PASS — Free agent eligibility tracked: `free_agent_eligible=1` for 524 players; `service_time_days` also present
- SKIP — When MLB roster drops below 23: call-up fires within 1 sim tick: Requires live roster depletion trigger
- SKIP — Called-up player comes from AAA first, AA second: Requires live event
- SKIP — Called-up player's level field updated from AAA to MLB: Field name mismatch (`minor_level` not `level`)
- SKIP — Called-up player appears on team's 25-man roster: Requires live event
- SKIP — Send-down trigger (OPS < .560 over 20+ AB): Requires live sim
- SKIP — Sent-down player's level updated to AAA: Requires live event
- SKIP — Sent-down player removed from 25-man roster: Requires live event
- SKIP — Player with no options remaining: DFA'd instead of sent down: Requires specific scenario

---

## Group 6 — Prospect Development — 2 Pass / 0 Fail / 4 Skip

- PASS — GET /api/teams/:id/minors returns structured data: Status 200; returns objects with `minor_level`, `stats` (includes `games`, `ab`, `avg` for batters; `ip`, `era` for pitchers)
- PASS — Minor leagues organized by level: Response keyed by "AAA", "AA", "A", "Rookie" with player lists
- SKIP — After 10 games simmed: at least one minor leaguer has a rating change logged: Requires live sim with development tick
- SKIP — Rating increases are 0-1 points per 10-game tick: Requires dev_tick observation (51,068 `dev_tick` transactions exist — suggests system fires regularly)
- SKIP — Player reaching age 26 still in AA with potential C/D: potential set to D, overall capped at 65: Requires specific player check
- SKIP — Analytics GM teams show higher average prospect rating growth over full season: Requires full season comparison

---

## Group 7 — Market Dynamics — 2 Pass / 0 Fail / 6 Skip

- PASS — At least 3 trades complete across league per season: 42 total trades confirmed in `transactions` table across 7 seasons (avg 6 per season)
- PASS — Non-tender: at least one player non-tendered per offseason: 205 `non_tender` transactions confirmed
- SKIP — By game 30: small market teams 8+ games back have veteran available in trade logic: Requires trade_available flag or game-30 snapshot
- SKIP — Trades show buyer/seller pattern: Requires player age cross-reference with trade transactions
- SKIP — Large/mega market buyer teams net exporters of prospects: Requires market_size × trade direction analysis
- SKIP — Small/medium market seller teams net importers of prospects: Requires full trade analysis
- SKIP — Interim GM teams skip trade deadline evaluation: Requires interim GM scenario during deadline
- SKIP — Non-tendered players enter free agent pool, not waiver wire: All 0 players in waiver state; consistent

---

## Group 8 — In-Season Firings — 2 Pass / 0 Fail / 9 Skip

- PASS — front_office_events table logs all firings with event_type, departing_person, incoming_person: 27 total firing events (22 `manager_fired`, 5 `gm_fired`) with departing/incoming names populated. Sample: "Tyler Taylor" → "Interim Manager"
- PASS — Interim manager/GM fields exist in teams table: `interim_gm` and `interim_manager` columns confirmed; 0 interim GMs, 2 interim managers currently active
- SKIP — Sim a team to 8+ games under .500: firing evaluation fires within 5 games: Requires targeted sim
- SKIP — Meddling owner fires manager before patient owner: Requires owner personality comparative test
- SKIP — Aggressive GM fires manager sooner than conservative GM: Requires GM type comparative test
- SKIP — Non-meddling owner: GM fires manager when threshold met: Requires owner scenario
- SKIP — Meddling owner: fires manager directly, bypasses GM: Requires meddling owner scenario
- SKIP — Owner fires manager directly (non-meddling): only at 2.5x base threshold: Requires threshold test
- SKIP — Owner firing manager directly (non-meddling): GM job_security -= 2: Requires job_security tracking
- SKIP — Interim manager: bench coach promoted, manager_style unchanged, all ratings -10: Requires interim promotion
- SKIP — Interim GM: conservative/moderate defaults, tenure = 0: Requires interim GM scenario

---

## Group 10 — Persistence — 1 Pass / 0 Fail / 4 Skip

- PASS — Server restart: existing data preserved (server loaded from DB on start): Server log confirmed "[engine] Restored league 1 (Test Dynasty League)"; 3553 games intact post-restart
- SKIP — Kill server mid-waiver window: waiver claim window resumes with correct games remaining: No active waiver windows to test
- SKIP — Kill server mid-season with interim GM: interim GM status preserved: No interim GMs currently active
- SKIP — Kill server after firing: front_office_events entry survives restart: Would require triggering then killing mid-season
- SKIP — Service time counters survive server restart: service_time persisted in DB; presumed persistent; requires before/after verification

---

## Group 11 — Error Handling — 3 Pass / 2 Fail / 2 Skip

- PASS — GET /api/waivers returns 200 with empty array when no players on waivers (not 404): Status 200, body `[]`
- PASS — GET /api/news returns 200 with empty array before any events generated: Status 200, `[]`
- PASS — GET /api/news?type=invalid returns 400 with `{"error": "Invalid event type filter"}`: Exact match confirmed
- FAIL — `[data-testid="news-ticker"]` is NOT present in the DOM on any tab. The implementation uses `[data-testid="game-ticker"]` which shows recent game scores. `game-ticker` is only visible on the League tab, not all tabs. This is a testid mismatch vs spec.
- FAIL — `[data-testid="news-tab"]` is NOT a top-level navigation element. Navigation uses `[data-testid="nav-news"]`. A `[data-testid="news-tab"]` element appears contextually inside the News view (as a header/label), not as the clickable nav button. The spec intended the nav button to have this testid.
- SKIP — Call-up attempted with no eligible minor leaguers: server logs warning, does not crash: Requires specific trigger
- SKIP — Trade attempted with interim GM team: trade skipped, logged to console, no crash: Requires interim GM scenario

---

## Schema Mismatches vs Test Spec (BUGS)

| Spec Assumed Column | Actual Column | Table | Impact |
|---|---|---|---|
| `level` (e.g. 'MLB','AAA','AA') | `minor_level` (NULL for MLB) + `is_on_mlb_roster` | players | Groups 5, 6 |
| `is_on_40man` | Does not exist | players | Group 3 |
| `fa_eligible` | `free_agent_eligible` | players | Group 5 |
| `type` in transactions | `transaction_type` | transactions | Groups 3, 7 |
| `flavor` in transactions | `narrative` (procedural text, not LLM flavor) | transactions | Group 1 |
| standalone `waivers` table | `waiver_state` + `claim_game_window_end` columns on `players` | — | Group 4 |
| `data-testid="news-ticker"` | `data-testid="game-ticker"` (games only, League tab only) | UI | Groups 0, 11 |
| `data-testid="news-tab"` (nav) | `data-testid="nav-news"` (nav button) | UI | Groups 0, 11 |

---

## Additional Observations

- ANTHROPIC_API_KEY is missing/malformed: server falls back to procedural text for all LLM-dependent features. LLM tests cannot be evaluated.
- `news_items` table has 0 rows — no news events generated in Season 7 state (playoffs/paused).
- 51,068 `dev_tick` transactions confirm prospect development system is actively firing.
- Current state is Season 7, playoffs, paused — many game-state tests require active simulation.
- 3 active `owner_sold_team` events confirm ownership transfer system fires correctly.
- 2 teams currently have `interim_manager=1` confirming interim promotion system works.
