# UI Tester B Results — Baseball Dynasty Simulator v0.2.0

**Date:** 2026-05-24  
**Worker:** B  
**Groups tested:** Group 9 (News Feed UI) + Additional (Waivers/Minors)

---

## Group 9 — News Feed UI — 11 Pass / 8 Fail / 8 Skip

### Itemized Results

| # | Test | Result | Notes |
|---|------|--------|-------|
| 9.1 | `[data-testid="news-tab"]` present in main navigation | **FAIL** | Spec requires `news-tab` in nav. Implementation uses `nav-news` for the nav link. `news-tab` exists only as the h2 heading element inside the news page itself, not as a navigation element. |
| 9.2 | `[data-testid="news-ticker"]` visible on all tabs during active sim | **FAIL** | Spec requires `news-ticker`. Implementation uses `game-ticker` which is only visible on the league/standings page. Not present in DOM at all on nav-teams, nav-games, nav-players pages. |
| 9.3 | News ticker shows last 5 events, updates as sim runs | **FAIL** | `game-ticker` (implementation's ticker) shows 20 items (game-ticker-item-{id}), not 5. Spec requires ≤5 events with `news-ticker` testid. Sim was paused so update behavior not verified. |
| 9.4 | `[data-testid="news-feed"]` renders list of news items | **PASS** | Present on news page after navigating via nav-news. |
| 9.5 | `[data-testid="news-filter-all"]` present | **PASS** | Present on news page. |
| 9.6 | `[data-testid="news-filter-transactions"]` present | **PASS** | Present on news page. |
| 9.7 | `[data-testid="news-filter-frontoffice"]` present | **PASS** | Present on news page. |
| 9.8 | `[data-testid="news-filter-injuries"]` present | **PASS** | Present on news page. |
| 9.9 | `[data-testid="news-filter-milestones"]` present | **PASS** | Present on news page. |
| 9.10 | `news-filter-all` shows all event types (functional) | **PASS** | Click fires, 0 items shown (DB has no news events yet in v0.2.0 state). |
| 9.11 | `news-filter-transactions` filters to transaction events | **PASS** | Click fires, 0 items (no news events in DB). |
| 9.12 | `news-filter-frontoffice` filters to front office events | **PASS** | Click fires, 0 items. |
| 9.13 | `news-filter-injuries` filters to injury events | **PASS** | Click fires, 0 items. |
| 9.14 | `news-filter-milestones` filters to milestone events | **PASS** | Click fires, 0 items. |
| 9.15 | Each `news-item-{eventId}` shows badge, headline, game number | **SKIP** | `/api/news` returns empty array — no news events in DB for current sim state (season 7, playoffs, paused). Cannot test item structure. |
| 9.16 | Click news item expands to full detail | **SKIP** | No news items to click. |
| 9.17 | Game result events in ticker show score only — no LLM headline | **SKIP** | `/api/news` returns `[]`. No game_result events to verify. |
| 9.18 | All non-game events have non-empty LLM headline string | **SKIP** | No news events in DB. |

---

## Additional Checks — 0 Pass / 2 Fail / 1 Skip

| # | Test | Result | Notes |
|---|------|--------|-------|
| A.1 | `waivers-list` element present when on waivers UI | **FAIL** | No `waivers-list` testid found anywhere in the app. No dedicated waivers page or section found. `/api/waivers` returns `[]` (no players on waivers). Roster tab on team page has no waiver section. |
| A.2 | `waiver-player-{playerId}` elements render | **SKIP** | `waivers-list` parent element not found. |
| A.3 | `minors-stats-{playerId}` elements render on minors tab | **FAIL** | `team-minors-tab` exists and is clickable on team detail panel. Minors section renders player list (AAA/AA with name, position, overall visible). However no `minors-stats-{playerId}` testids are attached. Players rendered without data-testid attributes. Spec requires `minors-stats-{playerId}` per player row. |

---

## DOM vs Spec Discrepancies

| Spec requires | Implementation has | Assessment |
|---|---|---|
| `data-testid="news-tab"` in main nav | `data-testid="nav-news"` in nav; `news-tab` only on news page heading | Implementation mismatch — rename needed |
| `data-testid="news-ticker"` | `data-testid="game-ticker"` | Implementation mismatch — rename needed |
| news-ticker on all nav tabs | game-ticker only on league/standings page | Scope mismatch — needs to be global layout element |
| news-ticker shows ≤5 events | game-ticker shows 20 game-ticker-item-* | Count mismatch |
| `waivers-list` (somewhere in UI) | Not implemented | Missing UI component |
| `waiver-player-{id}` elements | Not implemented | Missing UI component |
| `minors-stats-{playerId}` on minors tab | Minors tab exists (`team-minors-tab`) but player rows lack testids | Missing testid attributes |

---

## Environment Notes

- Server: `http://localhost:3001` — started successfully from `dist/server/server/index.js`
- Client: `http://localhost:5173` — Vite dev server
- DB state: Season 7, playoffs phase, sim paused. Existing data (teams, standings, game_log) present. `/api/news` returns empty array — news events table is empty for this sim run.
- Playwright: 1.60.0, Chromium headless

---

## Summary

**Group 9 — 11 Pass / 8 Fail / 8 Skip**  
**Additional — 0 Pass / 2 Fail / 1 Skip**  
**Overall — 11 Pass / 10 Fail / 9 Skip**
