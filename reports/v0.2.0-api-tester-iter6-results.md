# v0.2.0 API Tester — Iteration 6 Results
**Date:** 2026-05-24
**Focus:** AB-19 build/packaging fix — migration path correction
**Tester:** API Tester (HTTP lane only)

---

## 1. Migration Fix Verification

### Fix in Place
Checked `dist/server/server/migrations/` — all 8 SQL files confirmed present at the correct runtime-read path:
```
001_init.sql
002_playoff_series.sql
003_draft_picks_unique.sql
004_team_abbreviation.sql
005_draft_picks_unique_v2.sql
006_player_draft_index.sql
007_v0_2_0_schema.sql
008_injury_return.sql
```

### Fresh Database Test
Started server with `DB_PATH=/tmp/test-fresh-iter6.db node dist/server/server/index.js`.

**Server log (fresh DB):**
```
WARNING: ANTHROPIC_API_KEY missing or malformed. LLM features will use procedural fallback.
[db] Applying migration 001_init.sql
[db] Applying migration 002_playoff_series.sql
[db] Applying migration 003_draft_picks_unique.sql
[db] Applying migration 004_team_abbreviation.sql
[db] Applying migration 005_draft_picks_unique_v2.sql
[db] Applying migration 006_player_draft_index.sql
[db] Applying migration 007_v0_2_0_schema.sql
[db] Applying migration 008_injury_return.sql
[engine] No active league found
[server] Baseball Dynasty server running on http://127.0.0.1:3001 (localhost only)
```

**Result: PASS.** All 8 migrations applied in order. Zero errors. The `[db] No migrations directory found` error from Iteration 5 is completely absent.

API responded: `Season: None waiverCount: 0` — server functional on fresh schema.

### Carried Database Test
Started server against the existing carried DB (no `DB_PATH` override).

**Server log (carried DB):**
```
WARNING: ANTHROPIC_API_KEY missing or malformed. LLM features will use procedural fallback.
[engine] Restored league 1 (Test Dynasty League), phase: regular_season, forced paused
[server] Baseball Dynasty server running on http://127.0.0.1:3001 (localhost only)
```

**Result: PASS.** No migration log lines at all — all migrations were already applied to this DB, so the migration runner correctly detected and skipped them. No `[db] No migrations directory found` error.

---

## 2. Group 0 — Foundational Tests

| # | Test | Result | Evidence |
|---|------|--------|----------|
| 1 | GET /api/state → 200 | **PASS** | HTTP 200 |
| 2 | /api/state has `waiverCount` field | **PASS** | `waiverCount field present: True` |
| 3 | GET /api/teams → 200 | **PASS** | HTTP 200, 20 teams returned |
| 4 | GET /api/waivers → 200 | **PASS** | HTTP 200, empty array `[]` |
| 5 | GET /api/news → 200 | **PASS** | HTTP 200, 50 items returned |
| 6a | GET /api/news?type=invalid → 400 | **PASS** | HTTP 400 |
| 6b | /api/news?type=invalid error body | **PASS** | `{"error":"Invalid event type filter"}` |

All 6 Group 0 tests: **PASS**

**State snapshot at test time:**
- `currentGameNumber`: 458 (pre-sim), advanced to 535 post-sim
- `phase`: regular_season → offseason
- `seasonNumber`: 22
- `waiverCount`: 0

---

## 3. Sim Crash Test

Started fast sim for 20 seconds, then paused.

**Server log during sim — ERROR scan:**
```
(no ERROR, Tick error, or no such column lines found)
```

The `no such column: injury_return_game` error that would indicate migration 008 was not applied is **completely absent**.

**State after sim:**
- `currentGameNumber`: 535 (advanced from 458)
- `phase`: offseason (sim ran full regular season, playoffs, and entered offseason)
- `seasonNumber`: 22

The sim ran through:
- Regular season completion (game 500)
- Full playoffs (4 DS games, 2 CS games, 1 WS)
- Season 22 champion determined (Puebla del Norte Herons)
- Full offseason: retirement (1,488 players), development (14,145 players), prospect promotion (343 players), free agency (587 released), front office changes, annual draft

**Result: PASS.** No `injury_return_game` column error. No server crash. Sim ran to completion.

**Note:** LLM batch calls failed as expected (ANTHROPIC_API_KEY missing in test env) — procedural fallback operates correctly, this is not a regression.

---

## 4. Comparison to Iteration 5

| Issue | Iteration 5 | Iteration 6 |
|-------|-------------|-------------|
| `[db] No migrations directory found` on fresh DB | **PRESENT (P0 blocker)** — migrations copied to `dist/server/migrations/` but read from `dist/server/server/migrations/` | **RESOLVED** — migrations now at correct path |
| `no such column: injury_return_game` during sim | Present as consequence of missing migration 008 | **ABSENT** — migration 008 applied correctly |
| Server startup (carried DB) | Depended on previously-applied migrations surviving; path mismatch meant any new DB or clean build was broken | Clean startup, correct schema detection |
| Package start script | Pointed to wrong entry (`dist/server/index.js` did not exist) | Fixed: `node dist/server/server/index.js` |
| All Group 0 endpoints | Functional only because carried DB happened to have schema from dev builds | Fully functional, schema guaranteed via correct migration path |

The P0 migration blocker from Iteration 5 is **fully resolved**.

---

## 5. Overall Assessment

**PASS — AB-19 fix is correct and complete.**

All verifiable objectives for Iteration 6 are met:

- `dist/server/server/migrations/` contains all 8 SQL files (packaging fix confirmed)
- Fresh DB startup: all 8 migrations applied in sequence, no errors
- Carried DB startup: no spurious migration errors, schema detected as current
- Zero `no such column` errors during sim
- All Group 0 HTTP endpoints pass (200s, correct 400 for invalid filter, correct error body)
- Sim advances through full game cycle without crash

The only non-blocking observation: LLM headline batch calls fail due to missing API key in test environment — this is expected and the procedural fallback handles it cleanly. News items are present (50 returned) but headlines are empty strings for this test run.

**No blockers. Build is ready for further iteration testing.**
