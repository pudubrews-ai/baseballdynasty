# Never commit
.env
.env.local
.env.*.local

# Database (anchored to repo root — does NOT match baseball-dynasty/server/data/)
/data/
*.db
*.db-shm
*.db-wal

# Reports (local agent output — not for public repo)
reports/

# Build output
dist/
node_modules/
.DS_Store
